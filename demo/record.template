p :: package tag("home");

// Models

User : "Model"
User : type : record {
    first: string;
    last: string;
    email: string;
    }

Product : "Model"
Product :: record {
    name: string;
    price: string;
    }

Store : "Model"
Store :: {
    name: string;
    location: string;
    }
